// EXACT BYTE SLICE — generated by make-slices.mjs, do not hand-edit.
//
// artifact : new-wxt — wxt 0.20.27 — vite 8.3.0 + rolldown 1.2.9 (the toolchain v1.1.0 shipped with)
// origin   : repro/ built with that wxt version; everything else identical
// bytes    : 74374
// sha256   : e9c0e4c36e2a25d2e02a058674f93c8c87c6a9c23545c9d68056479d8bce70aa
// expected : no shim — freeGlobal is falsy, the third operand runs, and Firefox blocks it


### first `Function(` in the bundle, at byte 5747
// Present in both builds. In old-wxt it is unreachable; in new-wxt it runs.
 self==`object`&&self&&self.Object===Object&&self,k=sn||cn||Function(`return this`)(),ln=typeof e==`


### lodash _root.js — the chain that decides everything, at byte 5611
// freeGlobal is bound to a BARE `global` — no shim exists anywhere in this bundle,
// so inside the Firefox content-script sandbox `global` is undefined, `self` is the
// Xray-wrapped page window (self.Object !== Object), and the third operand — the
// one the CSP blocks — finally runs.
sn=typeof global==`object`&&global&&global.Object===Object&&global,cn=typeof self==`object`&&self&&self.Object===Object&&self,k=sn||cn||Function(`return this`)()
